Create an enhanced RetentionWatcher module at `/Users/Anton/.openclaw/agents/dante-agent/projects/drama-pipeline/scripts/retention_watcher.py` with the following features:

## Requirements:

### Features:
1. **OAuth 2.0 flow** - Reuse YouTube uploader token from `~/.openclaw/credentials/youtube-token.json` if available
2. **Fetch metrics**: views, likes, comments, avgViewDuration, subscribersGained
3. **Calculate retention percentage** (AVD / video length)
4. **Identify cliff points** from retention graph data
5. **Generate daily/weekly performance reports**
6. **Alert if retention < 70%** (post-mortem trigger)
7. **Save winning formulas** (retention > 95%) to templates

### CLI Interface:
```bash
python scripts/retention_watcher.py --video-id ABC123  # Single video
python scripts/retention_watcher.py --date 2026-02-14  # All videos from date
python scripts/retention_watcher.py --weekly-report    # Weekly insights
```

### Output Structure:
- `analytics/YYYY-MM-DD.json` - Raw metrics
- `analytics/weekly-insights.md` - Human-readable analysis
- `templates/winning-formulas.json` - Saved high-performing video formulas

### Key Implementation Details:
1. Use Google API Client Library for YouTube Analytics API v3
2. Token path: `~/.openclaw/credentials/youtube-token.json`
3. Analytics directory: `projects/drama-pipeline/analytics/`
4. Templates directory: `projects/drama-pipeline/templates/`
5. Uploads tracking: Read from `projects/drama-pipeline/uploads/*.json`
6. Video duration parsing from ISO 8601 format (PT#M#S)
7. Retention graph data from `youtubeAnalytics.reports.query` with `audienceWatchRatio` metric
8. Include post-mortem trigger for videos with < 70% retention
9. Save winning formulas (retention > 95%) with metadata: title pattern, hook structure, duration, topic category

### Code Structure:
- `RetentionWatcher` class with methods:
  - `__init__()` - Load tokens, setup directories
  - `_authenticate()` - OAuth with token reuse
  - `get_video_analytics(video_id, start_date, end_date)` - Fetch metrics
  - `get_retention_graph(video_id)` - Get audienceWatchRatio data
  - `calculate_retention_percentage(video_id)` - AVD / video length
  - `identify_cliff_points(retention_data)` - Find drop-off points
  - `check_video(video_id)` - Full analysis with alerts
  - `check_date(date_str)` - All videos from a date
  - `generate_weekly_report()` - Weekly insights
  - `save_winning_formula(video_data)` - Save high performers
  - `generate_post_mortem(video_data)` - Analysis for low retention

### Output Format (analytics/YYYY-MM-DD.json):
```json
{
  "date": "2026-02-14",
  "videos": [
    {
      "video_id": "ABC123",
      "title": "Video Title",
      "metrics": {
        "views": 1000,
        "likes": 50,
        "comments": 10,
        "avg_view_duration_sec": 42,
        "subscribers_gained": 5
      },
      "retention": {
        "video_length_sec": 60,
        "retention_percentage": 70.0,
        "cliff_points": [{"time": 15, "drop": 20}],
        "graph_data": [...]
      },
      "status": "warning|good|excellent",
      "alert": "Retention below 70%"
    }
  ]
}
```

### Weekly Report Format (analytics/weekly-insights.md):
```markdown
# Weekly Performance Report
Generated: 2026-02-14

## Summary
- Videos Analyzed: 5
- Avg Retention: 75%
- Alerts: 1

## Top Performers
...

## Post-Mortems
...

## Winning Formulas Saved
...
```

Write the complete, production-ready Python script with proper error handling, logging, and CLI interface.
